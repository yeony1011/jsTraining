# One Day One JavaScript 10

다음 요구사항에 따라 **HTML 엘리먼트 랜덤 배치기(Random Element Shuffler)**를 구현하시오.
화면에는 여러 콘텐츠 ```<div>```와 해당 콘텐츠를 제어할 ```<button>``` 탭이 존재한다.
페이지가 로드될 때, 콘텐츠와 탭의 순서가 랜덤하게 섞이되, 각 버튼과 매칭되는 콘텐츠는 같은 순서로 유지되어야 한다.

## 상세 요구사항
- 모든 동작은 순수 자바스크립트로 구현한다.
- 초기 화면에는 ```<div>```와 ```<button>``` 요소가 존재한다.
- 페이지 로드시, ```<div>``` 콘텐츠와 ```<button>``` 탭의 순서를 같은 기준으로 랜덤하게 섞는다.
- 요소의 순서 변경 시, 콘텐츠와 버튼 매칭 관계는 그대로 유지되어야 한다.
- 기존 요소를 제거하고 새로 생성하는 방식이 아니라, DOM 조작을 통해 순서만 변경한다.
- ```<div>```와 ```<button>```이 여러 개일 경우에도 동작하도록 일반화한다.
- 검색을 통해 스스로 해결하며, AI를 활용해 코드를 생성하거나 회신받는 행위는 금지한다.

## STUDY
### 


## 구현완료
div를 담은 배열을 랜덤 돌리고, div와 button에 각각 dataset 속성을 넣어 매칭할 수 있게 했습니다.
div를 forEach로 순회하면서 filter로 div와 button의 dataset이 일치하면 만들어놓은 신규배열에 button을 담아 div와 button을 랜덤한 순서로 매칭시켰습니다.
배열관련한 메서드들을 이것저것 시도해보며 매칭하는 부분에 많은 시행착오를 겪었고 어려웠습니다 😭